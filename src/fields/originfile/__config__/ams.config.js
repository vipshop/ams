export const config = {
    BASE: 'CONFIG_FIELD',
    type: {
        default: 'originfile'
    },
    default: {
        type: 'text',
        default: '',
        props: {
            placeholder: '默认值'
        }
    },
};

export const defaults = {};
