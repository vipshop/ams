export const config = {
    BASE: 'CONFIG_FIELD',
    type: {
        default: 'textarea'
    },
    default: {
        type: 'text',
        default: '',
        props: {
            placeholder: '默认值'
        }
    },
};

export const defaults = {};
