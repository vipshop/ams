export const config = {
    BASE: 'CONFIG_FIELD',
    type: {
        default: 'color'
    },
    default: {
        type: 'text',
        default: '#4FEE30',
        props: {
            placeholder: '默认值'
        }
    },
};

export const defaults = {};
