export const config = {
    BASE: 'CONFIG_FIELD',
    type: {
        default: 'button'
    },
    default: {
        type: 'text',
        default: '',
        props: {
            placeholder: '默认值'
        }
    },
};

export const defaults = {};
