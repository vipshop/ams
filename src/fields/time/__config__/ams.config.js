export const config = {
    BASE: 'CONFIG_FIELD',
    type: {
        default: 'time'
    },
    default: {
        type: 'text',
        default: '',
        props: {
            placeholder: '默认值'
        }
    },
};

export const defaults = {};
